// import { pgTable, serial, varchar } from "drizzle-orm/pg-core";

// export const procedure = pgTable("procedures", {
//   id: serial("id").primaryKey(),
//   name: varchar("procedure_name").notNull(),
// });
